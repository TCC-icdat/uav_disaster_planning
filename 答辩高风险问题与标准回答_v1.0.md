# 答辩高风险问题与标准回答 v1.0

以下回答均以冻结模型、E1–E5、h敏感性、Canonical案例和已核对文献为边界。每题建议先给结论，再给一项最关键证据，最后主动说明限制。

## 1. 你的创新到底在哪里？

第一项贡献是面向EO动态发现的SAR精查任务，建立全任务必做、以加权发现—完成延迟为主目标并纳入固定翼可行有向边代价的模型。第二项贡献是面向该模型设计事件触发修复：冻结已执行/当前执行状态，只开放少量受影响UAV的自由后缀，并用质量、计算时间和计划扰动三维证据验证折中；我不把优先级、Dubins或局部重规划单独宣称为首次。

## 2. 为什么不用GA或ACO？

本文重点不是发明通用元启发式，而是验证任务目标、固定翼代价和动态受限修复机制。Regret-2加多邻域局部搜索能统一服务Initial、Full和Local，使E4/E5只改变开放范围，因而更容易隔离Local机制的效果。

## 3. 为什么不和GA或ACO比较？

GA/ACO不是本文两项贡献的直接机制基线，仅增加某个通用求解器并不能直接回答“目标是否有效”或“受限后缀相对全后缀的折中如何”。当前E1用Exact Enumerator校验小规模求解质量，E2/E3隔离建模选择，E4/E5用同一求解器公平比较NoReorder、Full和Local，证据链已经闭合。

## 4. 为什么不直接与APR或CBBA-LR做代码级比较？

这些工作在任务可选性、目标函数、平台动力学、执行状态和扰动定义上并不等价，未经同构重实现的代码级数值比较容易失真。本文把它们作为贡献边界和机制先例，不宣称首次Local；数值实验则用共享目标、共享执行模型和共享启发式的三种策略隔离“限制自由后缀范围”这一因素。

## 5. 为什么使用weighted delay？

任务在不同时间被EO发现，真正关心的是从发现到完成精查的响应时长，而不是仅看所有任务最后何时结束。E2中该目标使weighted mean delay和high-priority mean delay分别改善约17.95%和29.39%，同时travel和makespan约增加3%，说明它表达的是明确的时效—资源取舍。

## 6. 为什么全部任务必须完成？

本文把已释放AOI定义为已经确认需要SAR精查的重点任务，因此它们不是可以为收益而放弃的候选点。全任务必做使优化关注“何时完成、由谁完成和按什么顺序完成”，并与可选奖励型任务分配工作形成清楚的问题边界。

## 7. 为什么选择h=2？

在Medium、50任务、5 UAV的冻结敏感性实验中，h从2增至3只带来约0.43%的平均相对质量收益且95% CI跨0，但runtime均值约增至2.60倍，assignment和successor扰动也明显增加。因此h=2只被选为代表性合理折中，不声称它是全局或所有规模下的最优参数。

## 8. Full不是最优，为什么还拿来比较？

Full的作用是控制求解器不变，只把可调整范围从少量受影响UAV扩展到全部UAV自由后缀，从而隔离“局部化”带来的质量、计算和扰动变化。它是更充分重算的启发式基线，不是Exact optimum、理论上界或全局最优。

## 9. 卫星已经能做灾害遥感，为什么还需要UAV？

本文把卫星定位为上游宏观态势和部分初始重点区域来源，不认为UAV替代卫星。固定翼EO/SAR UAV用于局部灾区持续初筛和按需精查，补充时空分辨率、持续更新和局部任务执行能力；二者是分层互补关系。

## 10. Canonical为什么不是现实实测？

Canonical使用可复现的几何、任务释放、服务模式和静态NFZ仿真，没有现场传感器标定或实飞数据，因此只能称为端到端案例研究。它证明链路与执行语义在该案例中一致可行，不证明真实灾区效果或统计优越性。

## 11. 为什么EO不和SAR联合优化？

本文问题入口是EO发现事件，研究对象是发现后SAR任务的异构固定翼动态规划与重规划；把EO覆盖路径同时纳入会改变决策变量、目标和贡献边界。Canonical中EO安全扫掠用于生成真实的释放时刻，但EO路径并不是本文在线联合优化核心。

## 12. NFZ避障为什么这么简单？

本研究用静态NFZ和安全膨胀检查保证Canonical路径不与禁飞区相交，目的是使任务规划结果在案例中具有基本几何可执行性。NFZ routing不是核心创新，本文也不声称动态障碍处理、复杂地形避障或全局最优路径能力。

## 13. E1的Exact到底精确到什么程度？为什么只到7个任务？

Exact Enumerator枚举2架UAV下全部任务排列及有标号路线切分，对冻结目标和约束的有限离散空间取全局最小值。枚举方案从5任务的720增至7任务的40320，平均时间从约0.043秒增至3.439秒，组合增长明显；因此E1只用于小规模质量校验，不替代大规模精确算法。

## 14. E3中R=5为什么没有稳定优势？

R=5时转弯约束较弱，欧氏规划得到的顺序在Dubins执行下往往仍接近可行几何排序，因此平均改善只有约0.98%，95% CI跨0。R增至10和15后改善升至约7.22%和17.28%，所以正确结论是“约束越强，忽略几何的损失越明显”，不是所有场景显著占优。

## 15. Local相比NoReorder是否所有时效指标都更好？

不是。E5中Local的weighted mean delay在Medium和Large相对NoReorder改善，但Large的high-priority mean delay平均变化约为-0.82%且95% CI跨0；因此本文只主张主目标、计算和扰动之间的总体折中。

## 16. runtime结果是否可以推广到其他计算机？

不能把秒数直接当作跨平台常数，因为墙钟时间受硬件、软件环境和实现影响。本文采用同环境、同场景、配对seed的相对比较；E5中Local/Full runtime ratio从Small的0.580降至Large的0.039，支持的是相对扩展趋势。

## 17. SAR会不会与Search and Rescue混淆？

本文SAR始终指Synthetic Aperture Radar，即合成孔径雷达载荷；首次出现必须中英文全称并给出缩写。引用Opti-UAV SAR等工作时会明确其SAR可能指Search and Rescue，避免把两个领域概念混成同一技术证据。

## 18. Canonical中计划扰动为零，是否与E4/E5矛盾？

不矛盾，Canonical是一个特定几何与释放序列下的端到端案例，零assignment/successor变化只是该实例的结果。计划扰动的统计行为由E4和E5的多seed Controlled Benchmark负责，不能用单个Canonical实例替代或否定。

## 19. 为什么distance-oriented只放补充材料？

在E2冻结场景中，distance-oriented在30个seed里均只激活1架UAV，虽然总飞行距离导向明确，却造成weighted mean delay约75.34，主要反映任务聚集的结构性退化。正式因果对照应是time-aware与mission-completion，因为二者更直接对应“响应时效—任务总体代价”的研究问题。

## 20. 两项贡献是不是已有组件的简单拼接？

相关组件确有先例，因此论文不争夺单组件首创，而把贡献限定在特定问题语义、模型组合、执行连续机制和完整实证链。E2/E3分别验证目标与固定翼代价，E4/E5再验证受限后缀的三维折中，Canonical只补充端到端一致性，这不是用案例替代算法证据。

## 21. 9个隐藏AOI如何产生？是否由脚本预先泄漏给规划器？

隐藏AOI的位置属于场景真值，但在EO安全扫掠实际覆盖到之前不会进入SAR规划器的已知任务集合；其release time由覆盖检测事件产生。3个t=0 AOI才是由卫星遥感、历史GIS和地面报告形成的多源先验输入。

## 22. 为什么可以停止补实验？

两项核心贡献均有直接对照，baseline共享目标评估和执行模型，E5共90个run全部完成且可行，h已有参数敏感性，raw复算与正式报告一致。当前高风险问题都能用既有证据回答；继续加实验只会扩展工作量，并不修补实质证据缺口。
